[package]
name = "niri-workspaces"
version = "0.1.0"
edition = "2021"

[dependencies]
niri-ipc = { version = "25.11.0", features = ["clap"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[profile.release]
lto = true
strip = true
opt-level = 3
